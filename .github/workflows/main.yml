name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Build ship
      run: |
        ls
        { echo 'ship-2'; echo 'Koa'; echo 'React'; } | bash <(curl -s https://raw.githubusercontent.com/paralect/ship-merge/master/ship-merge.sh)
      
    - name: Commit files
      run: |
        ls
        cd ship
        ls
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "Add changes"
        git log

    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.REPO_ACCESS_TOKEN }}
        force: true
        branch: 'main'
        repository: 'Oigen43/ga-test'
